<div class="comments">
    {{#each comments}}
    <section class="comment comment-content">
    <div class="meta">
      {{user.username}} on {{format_date created_at}} said:
    </div>
    <div class="text">
      {{comment_text}}
    </div>
  </section>
  {{/each}}
</div>