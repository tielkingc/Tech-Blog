<div class="container">
  <div class="d-flex flex-wrap justify-content-center py-3 px-5 mb-4">
    {{#each posts as |post|}}
  
    {{> post-info post }}
    <span class="nav-item pill edit"><a href="/dashboard/edit/{{post.id}}" class="nav-link text">Edit post</a></span>
  {{/each}}
  </div>
</div>